<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hi <%= @assign.user.email %></h1>
    <p>
      <a href="<%= @url_matcher %>"><%= @matcher.account.display_name %></a> tought
      this <a href="<%= @url_resource %>">resource</a> could be useful for your
       <a href="<%= @url_assign %>">assignment</a> .
      Please take a look and confirm the match if you find it useful too.
    </p>

    <p>
      Have a resourceful day!
    </p>
  </body>
</html>
